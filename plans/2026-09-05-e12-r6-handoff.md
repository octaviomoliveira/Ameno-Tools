# Handoff E12 — R6 integração e aceitação

Data: 2026-09-05
Branch: `feature/e12-input-recovery`
Status: concluído e aprovado para a E12-R; nenhum merge em `main` foi feito.

## Evidência automatizada

- `tools/validate-package.ps1`: pacote válido para 3ds Max 2026.
- Com o Max interativo fechado, 11/11 suítes do lote passaram em Batch 3ds Max
  2026.3; a suíte R2 também passou novamente (`33/33`).
- Contagens relevantes: E12-A `21/21`, E12-B `29/29`, E12-R1 `54/54`, E12-R2
  `33/33`, E12-R3 `28/28`, E12-R4 `21/21` e E12-C `48/48`. E10.7, E11.5 e o
  teste pelo `ApplicationPlugins` também passaram.
- Logs por suíte: `work/r6-test-logs` (diretório de saída ignorado pelo Git).

## Gate manual aprovado

O usuário abriu o 3ds Max, repetiu a cadeia Horizontal, iniciou a Vertical usando
o mesmo vértice direito da geometria original e confirmou: “tudo funcionando”. O
roteiro cobriu o encerramento no clique vazio, o reuso do vértice sem selecionar a
linha/texto da anotação e `Esc`/botão direito/`Ctrl+Z`/`Ctrl+Y`.

Isso encerra a aceitação interativa da E12-R. O pacote instalado continua sendo o
R4 funcional (`688c8bc`); os commits posteriores são apenas documentação e fixture
de teste, portanto não exigem reinstalação para este gate.

## Estado de publicação

O branch local está limpo e três commits estão à frente de
`origin/feature/e12-input-recovery`:

- `c57baff` — decisão de escopo do R5;
- `6cfdc53` — fixture E12-A com modo congelado explícito;
- `6afd610` — normalização do handoff R5.

Publicação desses commits requer autorização explícita. Depois dela, a próxima
atividade é opcionalmente preparar um merge revisado para `main`; não há merge
automático nesta entrega.
