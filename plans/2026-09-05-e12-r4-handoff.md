# Handoff E12 — R4 transação e rollback

Data: 2026-09-05  
Branch: `feature/e12-input-recovery`  
Commit de implementação: `2a7d8366130853605f571056ccadefe436191c29` + correção de Undo pendente de commit
Status: correção implementada e validada em Batch; reinstalação e gate final pendentes.

## Contrato implementado

- Cada tentativa de criação registra controller, linha, texto e marcador imediatamente após a alocação.
- Falhas em controller, linha ou texto propagam o erro original e removem todas as alocações da tentativa, sem tocar em objetos preexistentes.
- Em uma falha dentro de um `undo ... on` externo, o rollback não aninha `with undo off`; a transação externa restaura a tentativa sem deixar o `theHold` suspenso.
- A criação de preview também usa o mesmo registro transacional.
- Sucesso encerra e limpa o registro; rollback roda com `undo off` e não altera o histórico do usuário.

## Evidência

- `test_e12_r4_transaction.ms`: 21/21 PASS, incluindo falha aninhada e `theHold` não suspenso.
- `test_e12_chain_commit.ms`: 48/48 PASS, incluindo falha gráfica dentro do Undo real da cadeia e Undo/Redo H/V.
- `test_installed_package.ms`: PASS pelo `ApplicationPlugins` na versão anterior; repetir após reinstalação.
- Pacote: 26/26 arquivos conferidos por SHA-256.
- Backup da R3: `D:\Ameno\_backups\AmenoTools-before-2a7d836-20260905-191950`.

## Gate final

Publicar o commit R4 e, no Max, confirmar uma cadeia válida, uma falha/retry e Undo/Redo sem resíduos. Não fazer merge em `main`.
