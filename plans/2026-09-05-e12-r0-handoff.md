# Handoff E12-R0 — diagnóstico da cota contínua

Data: 2026-09-05  
Repositório: `https://github.com/octaviomoliveira/Ameno-Tools`  
Branch: `feature/e12-input-recovery`  
Worktree: `D:\Ameno\_worktrees\e12-input-recovery`  
HEAD executável instalado: `e1bc1142306b86f45a724cc42bf54fa41104c2e2`

## Estado objetivo

O gate **R0 foi concluído** em 2026-09-05. O trace real comprovou que o commit Horizontal cria a cadeia, mas não encerra o MouseTool, e que o modo Alinhado é aceito na captura antes de ser recusado pelo commit. Nenhuma correção funcional de R1–R6 foi implementada neste gate.

## O que foi concluído

1. Foi criada uma worktree própria na branch `feature/e12-input-recovery`, sem editar a worktree do Antigravity nem a main.
2. O commit `df8c69b` reverteu exclusivamente a tentativa `d8ce420`. O código de execução voltou ao comportamento de `ba55d95`, preservando a documentação posterior.
3. O commit `c076ca3` adicionou um build diagnóstico opt-in:
   - `Contents/scripts/ameno/core/ameno_continuous_diagnostics.ms`;
   - carregamento pelo bootstrap;
   - eventos de `MouseTool`, classificação, pedido de commit, criação, erro, rollback e cleanup;
   - limitação de logs repetidos de movimento;
   - logger desligado por padrão e protegido para não substituir o outcome do handler.
4. Foi criado `tests/maxscript/test_e12_r0_diagnostics.ms` para testar start/stop, persistência, throttling e preservação do outcome injetado.
5. Foi criado `docs/e12-r0-diagnostics.md` com o roteiro manual e interpretação do gate.
6. `PLAN.md` e `plans/2026-09-05-e12-executor-runbook.md` foram atualizados.
7. `tools/validate-package.ps1` passou no commit `c076ca3` antes do handoff.
8. A branch foi publicada no GitHub. O worktree estava limpo após o commit.
9. Os problemas de bootstrap restritos ao probe foram corrigidos em `e1bc114`: chamadas keyword de `trace`, leitura de ambiente por `System.Environment` e assinatura posicional consistente do hook `diagnosticTrace`.
10. Passaram serialmente `test_e12_r0_diagnostics.ms`, input, math, commit, continuous e bootstrap; `validate-package.ps1` também passou.
11. O build `e1bc114` foi instalado após backup em `D:\Ameno\_backups\AmenoTools-before-e1bc114-20260905-134400`; 25/25 arquivos corresponderam, sem ausentes, extras ou hashes divergentes.
12. O trace `%LOCALAPPDATA%\AmenoTools\Diagnostics\e12-r0-20260905-165149-560.log` (SHA-256 `598CFF70DBE9B695CE2A360FB71D5C1D1D02CAF4D1DE22EEDC0144126B75D2AA`) comprovou as fronteiras descritas em `docs/e12-r0-diagnostics.md`.

## O que permanece pendente

- R1–R6 não começaram e não houve merge para main.
- A falha de saída após `commitSuccess` deve ser corrigida no R1, retornando `#stop` na borda correta e mantendo cleanup idempotente.
- Alinhado deve ser bloqueado antes da captura no R1; este ciclo não implementa suporte Alinhado.
- A falha relatada ao reutilizar numa Vertical um vértice já usado numa Horizontal não foi reproduzida no trace, que não contém sessão `vertical`; investigar separadamente no gate de picking, sem atribuir causa prévia.

## Estado da instalação

A instalação diagnóstica ativa corresponde ao commit `e1bc1142306b86f45a724cc42bf54fa41104c2e2` desta worktree. Ela preserva o comportamento funcional de `ba55d95` e acrescenta o probe R0 corrigido. Não usar `tools/install-dev.ps1`, pois ele aponta para `D:\Ameno\_tools`.

## Próximo gate recomendado

1. Iniciar somente o R1 conforme `plans/2026-09-05-e12-executor-runbook.md`.
2. Corrigir o lifecycle independente já comprovado: sucesso deve retornar `#stop` na borda do MouseTool, sem confirmar em `mouseAbort` e sem apagar cotas confirmadas no cleanup.
3. Bloquear Alinhado antes de iniciar a captura, com mensagem clara, sem alterar a cota simples e sem implementar suporte Alinhado neste incremento.
4. Criar e executar `test_e12_r1_lifecycle.ms`, regressões pertinentes e gate real de saída.
5. Manter a reutilização Horizontal→Vertical como caso separado para R2/picking, salvo se o R1 produzir evidência direta e testável de dependência.

## Possíveis resultados do trace

- `mousePoint` não chega: problema de captura/evento.
- chega, mas classifica `ambiguous` ou `geometryWithoutReference`: problema de picking/snap, candidato inelegível ou preview interferindo.
- classifica `empty`, mas não aparece `commitRequestAccepted`: problema de estado/quantidade.
- aceita commit e registra `commitException`/`commitIncomplete`: problema na criação/rollback.
- registra `commitSuccess`, porém não aparece `stop` e a ferramenta continua ativa: problema de contrato de lifecycle/retorno do MouseTool.

Esses são critérios de leitura, não diagnósticos antecipados.

## Restrições

- Não matar o 3ds Max nem automatizar cliques na cena do usuário.
- Não usar logs antigos como evidência do teste novo.
- Não supor que os testes de 43/43 cobrem MouseTool real.
- Não alterar E11, renderizadores, cota simples ou núcleo E10 nesta etapa.
- Não implementar confirmação no `mouseAbort`; Esc e botão direito chegam pela mesma fronteira documentada.
- Não deduplicar cliques por tempo ou inferir clique físico apenas pela paridade de `clickNumber`.
- MAXScript: não usar `on`, `off`, `true` ou `false` como variáveis; callbacks .NET devem ser globais; parentetizar `if` usado em concatenação.

## Critério de saída do R0

R0 concluído: Batch verde, commit exato instalado, trace real e causa demonstrada estão registrados. A publicação desta atualização documental encerra o gate; R1 continua como incremento separado.
